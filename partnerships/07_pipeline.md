# 07 — Pipeline

**Status:** _stub — framework scoped, content to fill._

**Purpose:** see every partnership conversation and its stage at a glance, so
nothing stalls silently and effort goes where it moves.

**Will cover:**
- **The stages** — Identified → Researched → Contacted → In conversation →
  Agreement → Active → (or Dormant/Declined).
- **Where it lives** — lightweight tracking in `data/roadmap/connections.md`
  (status per org, like The Roadie Clinic's "contacted — awaiting reply").
- **Roadmap tie-in** — how pipeline progress feeds `pa-4` (company partnerships),
  `dh-3` (contractor network), and `OD-8` (team-deployment model); promote real
  milestones via the promote helper.
- **Cadence** — a simple weekly/biweekly review of the pipeline.

**Connects to:** the operational record of 06's outcomes; its milestones flow into
the roadmap so the dashboard reflects partnership progress.

# GitHub Pages configuration for 50-year roadmap dashboard
#
# The site is served from the repository ROOT: index.html, app.js,
# styles.css, and roadmap.json all live at the top level. The Pages
# source folder is set in repo Settings > Pages, not here.
theme: null
plugins: []

# Set the site title (appears in browser tab)
title: Deadhang Labor — The Path to the Homestead

# Markdown processor
markdown: kramdown
